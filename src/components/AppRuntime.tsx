import React,{ReactNode,useEffect,useState}from'react';import{AppState,StyleSheet,Text,View}from'react-native';import*as Network from'expo-network';import*as LocalAuthentication from'expo-local-authentication';import*as SecureStore from'expo-secure-store';import*as Notifications from'expo-notifications';import{router}from'expo-router';import{Button,Card}from'@/components/ui';import{useSession}from'@/store/session';import{colors,space,type}from'@/theme/tokens';
const biometricKey='opesinsure.biometric_enabled';const safePaths=['/(customer)/(tabs)/policies','/(customer)/(tabs)/claims','/payment','/confirmation'];
export function AppRuntime({children}:{children:ReactNode}){const status=useSession(s=>s.status);const[online,setOnline]=useState(true);const[locked,setLocked]=useState(false);const unlock=async()=>{const enabled=await SecureStore.getItemAsync(biometricKey);if(enabled!=='true'){setLocked(false);return}const result=await LocalAuthentication.authenticateAsync({promptMessage:'Unlock OpesInsure',fallbackLabel:'Use device credential',disableDeviceFallback:false});setLocked(!result.success)};useEffect(()=>{let mounted=true;const check=async()=>{const state=await Network.getNetworkStateAsync();if(mounted)setOnline(state.isConnected!==false&&state.isInternetReachable!==false)};void check();const timer=setInterval(()=>void check(),10000);const app=AppState.addEventListener('change',next=>{if(next==='active'&&status==='authenticated')void unlock()});const notification=Notifications.addNotificationResponseReceivedListener(response=>{const path=response.notification.request.content.data?.path;if(typeof path==='string'&&safePaths.includes(path))router.push(path as never)});return()=>{mounted=false;clearInterval(timer);app.remove();notification.remove()}},[status]);if(locked)return <View style={styles.lock}><Card feature><Text style={styles.title}>OpesInsure is locked</Text><Text style={styles.body}>Authenticate with this device before accessing insurance and financial information.</Text><Button label="Unlock securely" onPress={()=>void unlock()}/></Card></View>;return <View style={styles.flex}>{!online?<View accessibilityRole="alert" style={styles.offline}><Text style={styles.offlineText}>Offline · viewing available screens only. Transactions are paused.</Text></View>:null}<View style={styles.flex}>{children}</View></View>}
const styles=StyleSheet.create({flex:{flex:1},offline:{backgroundColor:colors.warningSoft,paddingHorizontal:space.x4,paddingVertical:space.x2},offlineText:{...type.meta,color:colors.warningText,textAlign:'center'},lock:{flex:1,justifyContent:'center',padding:space.x5,backgroundColor:colors.neutral50},title:{...type.pageTitle,color:colors.navy950},body:{...type.body,color:colors.neutral600}});
